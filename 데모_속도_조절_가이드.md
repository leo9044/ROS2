# ACR 데모 속도 조절 가이드

## 1. 속도를 결정하는 값

충전 동작은 `acr_node.cpp`의 `kSteps = 200`회 명령으로 구성된다. 한 단계마다 `control_period_ms`만큼 대기하므로 대략적인 동작 시간은 다음과 같다.

```text
충전 시간(초) = 201 × control_period_ms ÷ 1000
```

기본값 `50 ms`는 약 10초다. 시연에는 `100 ms`(약 20초)를 권장한다. 관절 명령 수는 유지하므로 움직임의 부드러움은 유지되고 속도만 느려진다.

## 2. 재빌드 없이 런치에서 느리게 실행하기

```bash
cd ~/acr_ws
source /opt/ros/jazzy/setup.bash
source install/setup.bash
ros2 launch acr_core acr_system.launch.py control_period_ms:=100 car_start_delay_sec:=8.0
```

`control_period_ms:=100`은 충전 이동을 약 20초로, `car_start_delay_sec:=8.0`은 Gazebo·RViz가 먼저 완전히 보인 뒤 8초 후에 CAR_Node의 Service 인증과 Action 요청이 시작되도록 한다.

권장 값은 다음과 같다.

| 목적 | `control_period_ms` | 예상 이동 시간 |
| --- | ---: | ---: |
| 기본 데모 | 50 | 약 10초 |
| 발표·화면 녹화 | 100 | 약 20초 |
| 설명을 길게 할 때 | 150 | 약 30초 |

값이 너무 크면 Gazebo 물리 제어가 계단식으로 보일 수 있으므로 `200 ms` 이하는 권장한다.

## 3. MRM을 원하는 시점에 보이게 하기

MRM 시연에서는 장애물이 launch 시작 후 `obstacle_delay_sec`초에 생긴다. CAR_Node는 `car_start_delay_sec`초 후에 움직이기 시작하므로, 장애물이 충전 동작 중 나타나는 대략적인 시점은 다음과 같다.

```text
충전 시작 뒤 경과 시간 ≈ obstacle_delay_sec - car_start_delay_sec
```

20초 충전 동작의 중간쯤에 MRM을 보이려면 다음 명령을 사용한다.

```bash
ros2 launch acr_core acr_system.launch.py \
  spawn_obstacle:=true \
  control_period_ms:=100 \
  car_start_delay_sec:=8.0 \
  obstacle_delay_sec:=18.0
```

이 경우 CAR_Node가 약 8초에 시작되고, 사람형 장애물은 약 18초에 생성되어 약 10초 동안 이동한 뒤 MRM이 발생한다. 실제 Service 연결 시간에 따라 수백 ms 정도 차이가 날 수 있다.

## 4. Service와 Action 통신을 천천히 설명하기

아래 인자는 발표용 **의도적 지연**이다. 통신 실패를 기다리는 timeout이 아니라, 이미 준비된 통신의 각 단계를 관객에게 보여주기 위한 delay다. 기본값은 모두 `0.0`이므로 일반 동작에는 영향을 주지 않는다.

| 인자 | 지연되는 구간 |
| --- | --- |
| `car_start_delay_sec` | launch 뒤 CAR_Node 자체가 시작될 때까지 |
| `auth_request_delay_sec` | Service server 발견 → 인증 요청 전송 전 |
| `auth_response_delay_sec` | ACR이 인증 요청 수신 → 응답 전송 전 |
| `action_goal_delay_sec` | 인증 승인 응답 수신 → Action goal 전송 전 |

다음은 통신 흐름을 설명하기 좋은 정상 데모 예시다.

```bash
ros2 launch acr_core acr_system.launch.py \
  car_start_delay_sec:=8.0 \
  auth_request_delay_sec:=3.0 \
  auth_response_delay_sec:=2.0 \
  action_goal_delay_sec:=4.0 \
  control_period_ms:=100
```

화면 순서는 다음과 같다.

1. 0~8초: Gazebo/RViz와 HUD `STANDBY`를 설명한다.
2. 8~11초: CAR_Node가 Service를 찾았음을 로그로 보여준다.
3. 11~13초: 인증 요청이 ACR_Node로 전달되고, ACR_Node가 2초 뒤 응답함을 보여준다.
4. 13~17초: HUD `AUTHENTICATED`와 Service 승인 결과를 설명한다.
5. 17초 이후: Action goal 전송·수락, `MOVING TO CHARGE PORT: n%` Feedback을 설명한다.

## 5. 코드를 직접 바꿀 때의 위치

기본 제어 주기는 `src/acr_core/src/acr_node.cpp`의 아래 줄이다.

```cpp
control_period_ms_ = this->declare_parameter<int>("control_period_ms", 50);
```

기본 이동 단계 수는 같은 파일의 아래 줄이다.

```cpp
constexpr int kSteps = 200;
```

둘 중 하나를 직접 고친 뒤에는 반드시 다시 빌드한다.

```bash
cd ~/acr_ws
colcon build --packages-select acr_core
source install/setup.bash
```

일반적으로는 소스 수정 대신 2절의 런치 인자를 쓰는 편이 안전하다.

## 6. 화면에서 설명할 순서

1. Gazebo의 차량·ACR·사람형 장애물 위치와 RViz의 RobotModel·파란 LaserScan을 보여준다.
2. HUD의 `STANDBY` 상태를 보여준다.
3. CAR_Node가 시작되면 `AUTHENTICATED`를 보여주고, Service 인증 로그를 설명한다.
4. `MOVING TO CHARGE PORT: n%` HUD와 두 관절 움직임을 보이며 Action Feedback을 설명한다.
5. MRM 데모에서는 빨간 안전 반경 안으로 사람형 장애물이 들어오는 순간 `MRM ACTIVE`와 Action `ABORTED`를 함께 보여준다.
6. `ros2 param set /acr_node safety_distance 0.12`를 실행해 반경이 바뀌는 모습을 보여주면 Parameter 통신도 증명할 수 있다.
